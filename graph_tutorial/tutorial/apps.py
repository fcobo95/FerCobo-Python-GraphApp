from django.apps import AppConfig


class TutorialConfig(AppConfig):
    name = 'tutorial'
    
# This looks like the app itself, setting the name 'tutorial' to it, 
# as used in the other classes and part of the application.
